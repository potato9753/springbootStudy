<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.3.xsd">
	
	
	<!-- ///////////////////////MyBatis 연동/////////////////// -->

	<!-- 1. jdbc.properties 등록 -->
	<context:property-placeholder location="com/config/jdbc.properties" />

	<!-- 2. 4가지 정보이용해서 DB 연결 -->
	<bean id="MyDataSouce" class="org.apache.commons.dbcp2.BasicDataSource">
		<property name="driverClassName" value="${jdbc.driver}" />
		<property name="url" value="${jdbc.url}" />
		<property name="username" value="${jdbc.user}" />
		<property name="password" value="${jdbc.passwd}" />
	</bean>
	
	<!-- 3. 이전 SqlSessionFactory 역할 빈 등록 -->
	<bean id="MySqlSessionFactoryBean" class="org.mybatis.spring.SqlSessionFactoryBean">
		<property name="dataSource" ref="MyDataSouce"/>
		<property name="mapperLocations">
			<list>
			<value>com/config/DeptMapper.xml</value>
			</list>
		</property>
		<property name="typeAliases">
			<list>
				<value>com.dto.Dept</value>
			</list>
		</property>
	</bean>

	<!-- 4. 이전 SqlSession 역할 빈 등록 -->
	<bean id="MySqlSessionTemplate" class="org.mybatis.spring.SqlSessionTemplate">
		<constructor-arg name="sqlSessionFactory" ref="MySqlSessionFactoryBean"/>
	</bean>
	
	<!-- ///////////////////////MyBatis 연동////제어의 역행(Inversion of Control : IoC)//////////// -->
	<!-- 빈끼리의 연동방법은 외부에서 주입방법을 사용한다.(의존성 주입(Dependency Injection) 
		1. constructor 주입
		2. setter 주입
		
	모든 빈은 독립적인 빈(클래스)로 작성할 수 있다. => POJO(Plain Old Java Object)
	-->
	<!-- 
		base-package="com.*" 형식과 같이 ㅍ키지를 지정하면 자동으로 지정된 패키지내에 존재하는 빈들이 자동생성된다.
		단, @Service, @Repository, @Component 등과 같이 클래스 레벨로 지정된 빈들만 해당된다.
		
		component-scan내에 어노테이션 활성화 기능이 내장되어 있어 따로 어노테이션 활성화가 필요없다.
	 -->
	<context:component-scan base-package="com.*"/>

</beans>
